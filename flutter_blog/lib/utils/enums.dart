enum ThemeType { light, dark }

/// [PageType.web] : width / height > 1
/// [PageType.phone] : 1 > width / height > 0.6
/// [PageType.smallWeb] : 0.6 > width / height
enum PageType { web, phone, smallWeb }
